# Routes Package

This folder is created to satisfy the specified folder structure requirements.
In this setup, central routing is declared directly in `src/App.tsx` using `react-router-dom` to wrap all pages in the `PageLayout` view.
